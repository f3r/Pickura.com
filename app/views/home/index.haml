.cover_image#screen
  = image_tag current_user.current_photo.source.url, :id => 'user_image', :style => "z-index:10"
  
.profile_pic_wrapper
  .profile_pic
    .image
      = image_tag current_user.avatar.url, :id => 'user_avatar'

.sidebar_images

.form-actions
  = link_to "Save", root_path, :class => 'btn'
  = link_to "Customize", editor_path, :class => 'btn'